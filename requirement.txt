pip install azure-storage==0.30.0
pip install azure.function

import logging
import pandas as pd

    filenames.append(blob.name)
    print('1')
    for f in filenames:
        print('2')
        pd.read_excel(blob.name)
        print(dataframes)
    